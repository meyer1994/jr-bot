os: "linux"
dist: "bionic

language: "python"
python:
  - "3.7"
  - "3.8"
  - "3.8-dev"
  - "nightly"

cache:
  pip: "true"

jobs:
  allow_failures:
    - python: "3.8-dev"
    - python: "nightly"

install:
  - "pip install -r requirements.txt"

script:
  - "python -m unittest discover -vb tests"
